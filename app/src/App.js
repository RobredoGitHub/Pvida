/* import logo from './logo.svg';
import './App.css';
import { useState } from 'react';

const request = async (data) => {
    const result = await fetch('http://localhost:8080/form');
         const text = await result.text(data); 
         const { adaptado } = JSON.parse(text); 
    console.log(result);
    return result;
};
 */
